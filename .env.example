# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=hopedb

# Gemini API Configuration (optional - if not set, will use rule-based matching)
GEMINI_API_KEY=your_gemini_api_key_here
